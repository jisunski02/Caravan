<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ucu.cite.caravanrental"
    android:hardwareAccelerated="true">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/caravanlogo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caravan"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UploadPaymentActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCLLmjstpOHRJZLDkkKZw6K00WtHFDwxKY" />

        <activity
            android:name=".MapsRepairShop"
            android:exported="false"
            android:label="@string/title_activity_maps_repair_shop" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SMSOtpActivity"
            android:exported="false" />
        <activity
            android:name=".ViewRentDetails"
            android:exported="false" />
        <activity
            android:name=".MainscreenActivity"
            android:exported="false" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Pndng_Rents" />
        <activity android:name=".Apprvd_Rents" />
        <activity android:name=".Cmpltd_Rents" />
        <activity android:name=".displayDetails" />
        <activity android:name=".mylist" />
        <activity android:name=".enterRefNum" />
        <activity android:name=".lastpage" />
        <activity android:name=".selectmop" />
        <activity android:name=".review" />
        <activity android:name=".selectvehicle" />
        <activity android:name=".trydriverlist" />
        <activity android:name=".selectpackage" />
        <activity android:name=".Repair_Services" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".Car_Bookings" />
        <activity android:name=".Mainscreen" />
        <activity android:name=".profile" />
        <activity android:name=".PrivacyPolicy" />
        <activity android:name=".TermsOfService" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".HomeNotLoggedIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SMSOtpActivityOnProfile" />
        <activity android:name=".UserRents" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>